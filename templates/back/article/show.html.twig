{% extends 'back/baseBack.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Id_user</th>
                <td>{{ article.idUser }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_index_back') }}">back to list</a>

    <a href="{{ path('app_article_edit_back', {'id': article.id}) }}">edit</a>

    {{ include('back/article/_delete_form.html.twig') }}
{% endblock %}
